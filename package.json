{
  "name": "modusignal",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "preview": "python3 -m http.server 4173 --directory _site"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
